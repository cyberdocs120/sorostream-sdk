---
'@sorostream/sdk': patch
---

fix(connectionPool): add atomic `acquire()` bounded by `maxConnections` with FIFO queueing for excess waiters (issue #539)
fix(replayTransport): preserve recorded response ordering during replay and add a `sequential` option (default true) for optional parallel replay (issue #544)
fix(xdrValidation): throw typed `XdrValidationError` (extends `SoroStreamError`, with structured `code` field on malformed/mutated envelopes) instead of an untyped error (issue #546)
feat(client): add `pauseStream` and `resumeStream` methods, encoding and submitting the pause/resume instructions and returning the transaction hash (issue #549)